const arrayAddEvent = (e) => {
  console.log("Add");
};

const arrayDeleteEvent = (e) => {
  console.log("Delete");
};

const arrayInsertEvent = (e) => {
  console.log("Insert");
};

const arrayUpdateEvent = (e) => {
  console.log("Update");
};

export const CONTROLMENU = [];

export const renderModule = () => {};

const render = () => {};

export default render;
